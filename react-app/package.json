{
  "name": "learning01-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "rm -rf build && mkdir -p build && cp index.html build/index.html"
  }
}