<configuration scan="true">

    <!-- Spring Boot default console appender / patterns -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- SPLUNK HEC appender -->
    <appender name="SPLUNK" class="com.splunk.logging.HttpEventCollectorLogbackAppender">
        <url>https://localhost:8088</url> <!-- match your working HEC endpoint -->
        <token>2794206d-72a6-49ac-8ccd-283f0b4058bb</token>
        <index>main</index>
        <sourcetype>on-premise-app</sourcetype>
        <source>on-premise-app</source>
        <disableCertificateValidation>true</disableCertificateValidation>

        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} %-5level [%thread] %logger - %msg%n</pattern>
        </layout>
    </appender>

    <!-- Only your application logs go to Splunk + console -->
    <logger name="com.example" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="SPLUNK"/>
    </logger>

    <!-- Mute or nearly-mute everyone else -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework.kafka" level="ERROR"/>
    <logger name="org.apache.kafka" level="ERROR"/>
    <logger name="com.couchbase" level="ERROR"/>

    <!-- Root: only show catastrophic stuff from libs, no Splunk -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>